let velocidad = 270;
let altura = 180;

if (velocidad >= 268 && velocidad <= 278 && altura >= 150 && altura <= 300) {
    console.log("Velocidad y altura adecuados, puede aterrizar.");
    
} else {
    console.log("¡ATENCIÓN, NO PUEDE ATERRIZAR! La velocidad debe estar comprendida entre 268 a 278 km/h y la altura debe estar comprendida entre 150 a 300 m");
}